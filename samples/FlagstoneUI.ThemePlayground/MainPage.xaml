<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:fs="clr-namespace:FlagstoneUI.Core.Controls;assembly=FlagstoneUI.Core"
             x:Class="FlagstoneUI.ThemePlayground.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            
            <Label
                Text="Flagstone UI Theme Playground"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Material Design Theme Demo"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2" />

            <!-- Demonstrate Flagstone UI Card control with Material theme -->
            <fs:FsCard>
                <VerticalStackLayout Spacing="10">
                    <Label Text="Flagstone UI Card" FontAttributes="Bold" />
                    <Label Text="This card is styled using the Material Design theme and consumes design tokens from the Core library." />
                    <Button Text="Themed Button" HorizontalOptions="Start" />
                </VerticalStackLayout>
            </fs:FsCard>

            <!-- Standard MAUI controls with theme styles -->
            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

            <fs:FsCard>
                <VerticalStackLayout Spacing="10">
                    <Label Text="Theme Information" FontAttributes="Bold" />
                    <Label Text="• Uses tokens from FlagstoneUI.Core" />
                    <Label Text="• Material Design color scheme" />
                    <Label Text="• Cross-assembly resource loading working!" />
                </VerticalStackLayout>
            </fs:FsCard>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
