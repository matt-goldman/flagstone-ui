<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:fs="clr-namespace:FlagstoneUI.Core.Controls;assembly=FlagstoneUI.Core">
  <ResourceDictionary.MergedDictionaries>
    <!-- TODO: Fix resource reference to Tokens.xaml - see issue #12 -->
    <!-- <ResourceDictionary Source="/FlagstoneUI.Core;component/Styles/Tokens.xaml" /> -->
  </ResourceDictionary.MergedDictionaries>
  <Style TargetType="Button">
    <!-- TODO: Use DynamicResource once Tokens.xaml is properly referenced - see issue #12 -->
    <Setter Property="BackgroundColor" Value="#6750A4" />
    <Setter Property="TextColor" Value="#FFFFFF" />
  </Style>
  
  <Style TargetType="fs:Card">
    <!-- TODO: Use DynamicResource once Tokens.xaml is properly referenced - see issue #12 -->
    <Setter Property="Padding" Value="16" />
    <Setter Property="BackgroundColor" Value="#FFFBFE" />
    <Setter Property="BorderColor" Value="#79747E" />
    <Setter Property="CornerRadius" Value="8" />
  </Style>
</ResourceDictionary>
